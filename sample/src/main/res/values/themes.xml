<resources>
    <!-- Base application theme. -->
    <style name="Theme.AndroidBase" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="Theme.AndroidBase.Start" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/start_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_empty</item>
        <item name="postSplashScreenTheme">@style/Theme.AndroidBase</item>
    </style>

    <drawable name="statusBarBackground">@color/purple_700</drawable>
    <drawable name="titleBarBackground">@color/purple_500</drawable>
    <color name="titleColor">@color/white</color>
</resources>